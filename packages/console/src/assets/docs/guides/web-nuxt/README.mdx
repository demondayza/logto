import UriInputField from '@/mdx-components/UriInputField';
import NpmLikeInstallation from '@/mdx-components/NpmLikeInstallation';
import InlineNotification from '@/ds-components/InlineNotification';
import Steps from '@/mdx-components/Steps';
import Step from '@/mdx-components/Step';
import CustomDomainEndpointNotice from '@/mdx-components/CustomDomainEndpointNotice';
import Checkpoint from '../../fragments/_checkpoint.md';
import RedirectUrisWeb from '../../fragments/_redirect-uris-web.mdx';
import { generateStandardSecret } from '@myeyesid/shared/universal';

export const cookieEncryptionKey = generateStandardSecret();

<Steps>

<Step
  title="Installation"
  subtitle="Install MyEyesID SDK"
>

<InlineNotification severity="alert">
  MyEyesID Nuxt SDK only works with Nuxt 3.
</InlineNotification>

<NpmLikeInstallation packageName="@myeyesid/nuxt" />

</Step>

<Step title="Register MyEyesID module">

In your Nuxt config file, add the MyEyesID module and configure it:

<Code title="nuxt.config.ts" className="language-tsx">
{`export default defineNuxtConfig({
  modules: ['@myeyesid/nuxt'],
  runtimeConfig: {
    myeyesid: {
      endpoint: '${props.endpoint}',
      appId: '${props.app.id}',
      appSecret: '${props.secrets[0]?.value ?? props.app.secret}',
      cookieEncryptionKey: '${cookieEncryptionKey}', // Random-generated
    },
  },
  // ...other configurations
});`}
</Code>

Since these information are sensitive, it's recommended to use environment variables (`.env`):

<Code title=".env" className="language-bash">
    {`NUXT_LOGTO_ENDPOINT=${props.endpoint}
NUXT_LOGTO_APP_ID=${props.app.id}
NUXT_LOGTO_APP_SECRET=${props.secrets[0]?.value ?? props.app.secret}
NUXT_LOGTO_COOKIE_ENCRYPTION_KEY=${cookieEncryptionKey} # Random-generated
`}
</Code>

<br/>

<CustomDomainEndpointNotice />

See [runtime config](https://nuxt.com/docs/guide/going-further/runtime-config) for more information.

</Step>

<Step 
  title="Configure redirect URIs"
  subtitle="2 URIs"
>

<RedirectUrisWeb />

When registering `@myeyesid/nuxt` module, it will do the following:

- Add three routes for sign-in (`/sign-in`), sign-out (`/sign-out`), and callback (`/callback`).
- Import two composables: `useMyEyesIDClient` and `useMyEyesIDUser`.

These routes are configurable via `myeyesid.pathnames` in the module options, for example:

```ts title="nuxt.config.ts"
export default defineNuxtConfig({
  myeyesid: {
    pathnames: {
      signIn: '/login',
      signOut: '/logout',
      callback: '/auth/callback',
    },
  },
  // ...other configurations
});
```

Check out the [type definition file](https://github.com/myeyesid-io/js/blob/HEAD/packages/nuxt/src/runtime/utils/types.ts) in the `@myeyesid/nuxt` package for more information.

<InlineNotification>
If you configure the callback route to a different path, you need to update the redirect URI in MyEyesID accordingly.
</InlineNotification>

</Step>

<Step title="Implement sign-in and sign-out">

Since Nuxt pages will be hydrated and become a single-page application (SPA) after the initial load, we need to redirect the user to the sign-in or sign-out route when needed. To help with this, our SDK provides the `useMyEyesIDUser()` composable, which can be used in both server and client side.

```html title="index.vue"
<script setup lang="ts">
  import { useMyEyesIDUser } from '#imports'; // Add this line if auto-import is disabled
  const user = useMyEyesIDUser();
</script>
<template>
  <!-- Display user information when signed in -->
  <ul v-if="Boolean(user)"> 
    <li v-for="(value, key) in user"><b>{{ key }}:</b> {{ value }}</li>
  </ul>
  <!-- Simplified button for sign-in and sign-out -->
  <a :href="`/sign-${ user ? 'out' : 'in' }`"> Sign {{ user ? 'out' : 'in' }} </a>
</template>
```

</Step>

<Step title="Checkpoint: Test your app">

<Checkpoint />

</Step>

</Steps>
