# @myeyesid/shared

The shared package for **non-business** components. For MyEyesID business related stuff, put it to one of the packages in `packages/toolkit/`.

The main import includes all components which may require Node. Import `@myeyesid/shared/universal` for the universal bundle.
